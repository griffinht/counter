{
  "name": "counter",
  "type": "module",
  "scripts": {
    "dev": ". ./.env && vite",
    "build": "vite build",
    "run": ". ./.env && vite --port 7238",
    "oldpreview": "wrangler pages dev",
    "olddeploy": "npm run build && wrangler pages deploy"
  },
  "dependencies": {
    "@emotion/styled": "^11.14.0",
    "hono": "^4.6.20",
    "postgres": "^3.4.5",
    "typed-htmx": "^0.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.1",
    "vite": "^5.4.12",
    "wrangler": "^3.96.0"
  }
}
